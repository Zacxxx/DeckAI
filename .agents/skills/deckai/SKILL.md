---
name: DeckAI
description: Explain exactly when this skill should and should not trigger. Triggers when the user asks to generate slides, evaluate component aesthetics, apply DOM patches, or interact with DeckAI Canvas endpoints. Does not trigger for generic codebase queries.
---
You have access to the DeckAI MCP server which exposes internal tooling to render HTML elements natively into the slide canvas. 
Use the connected MCP endpoints to manipulate the local SQL database or invoke headless browser validations on UI bounds.
